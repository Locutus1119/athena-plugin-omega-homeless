import * as alt from 'alt-server';
export class TrashSystem {
    public static searchTrash(player: alt.Player, trashbinId: string, trashItems) {
        // alt.emitClient(player, `HomelessRP:Client:OpenVue`, identifier, trashItems);
    }
}
